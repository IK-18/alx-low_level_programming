#include "main.h"

/**
 * print_number - prints a number
 * @n: digit to print
 * Return: the number
 */

void print_number(int n)
{
	int th, h, t, d;

	if (n < 0)
	{
		n = -n;
		_putchar(45);
	}
	
	if (i / 10)
	{
		print_number(i / 10);
	}
	_putchar(i % 10 + '0');
}
