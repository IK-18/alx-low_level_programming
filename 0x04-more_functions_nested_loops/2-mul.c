#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: secind integer
 * Return: the product of both integers
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
